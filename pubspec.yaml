name: fresh_timer_pro
description: "食材保鲜计时Pro"
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.10
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true